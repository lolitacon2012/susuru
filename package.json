{
  "name": "Susuru",
  "version": "0.0.1",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "typings": "./dist/types/index.d.ts",
  "description": "Yet another frontend framework",
  "repository": "https://github.com/lolitacon2012/Susuru.git",
  "author": "Liu Dake <kannagi.cafe>",
  "license": "Unlicense",
  "scripts": {
    "demo": "yarn build:dist && serve demo",
    "clean:dist": "rm -rf demo/dist",
    "clean": "rm -rf dist && yarn clean:dist",
    "build:dist": "yarn clean:dist && rollup -c --environment target:demo",
    "build": "yarn clean && rollup -c --environment target:main && rollup -c --environment target:demo"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^21.0.2",
    "@rollup/plugin-json": "^4.1.0",
    "@rollup/plugin-node-resolve": "^13.1.3",
    "rollup": "^2.70.0",
    "rollup-plugin-polyfill-node": "^0.8.0",
    "rollup-plugin-size": "^0.2.2",
    "rollup-plugin-terser": "^7.0.2",
    "rollup-plugin-typescript2": "^0.31.2",
    "serve": "^13.0.2",
    "tslib": "^2.3.1",
    "typescript": "^4.6.2"
  }
}
